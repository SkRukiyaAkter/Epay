# Backend API
API_BASE_URL=http://localhost:5000

# Session encryption (iron-session)
# ⚠️ CRITICAL: Generate with: python -c "import os; print(os.urandom(32).hex())"
# This value must be kept secret and never committed to version control.
SESSION_SECRET=change-me-to-a-random-string-at-least-32-chars

# App name
NEXT_PUBLIC_APP_NAME=E-Payment MVP
